apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-control-plane
  namespace: 'argocd'
spec:
  destination:
    name: 'in-cluster'
    namespace: 'argocd'
  project: default
  source:
    path: argocd/control-plane
    repoURL: https://github.com/csantanapr/control-plane-eks
    directory:
      recurse: true
  syncPolicy:
    automated: {}